# Runtime dependencies — what the dashboard and the trading loop need.
#
# Streamlit Cloud installs this file on every build, so it is deliberately
# lean. Development-only tools (notebooks, plotting, the test runner) live in
# requirements-dev.txt and are not installed on the deployed app.

# Core data stack
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # Parquet cache

# Machine learning
scikit-learn>=1.4

# Dashboard
streamlit>=1.35
plotly>=5.20

# APIs
requests>=2.31
openai>=1.30
pydantic>=2.7          # structured LLM output validation
python-dotenv>=1.0

# NOTE: pandas-ta and TA-Lib are deliberately NOT used.
#   pandas-ta has NumPy 2.x incompatibilities and an uncertain maintenance
#   future; TA-Lib requires a C toolchain build. All indicators in
#   src/indicators/indicators.py are implemented directly in pandas, which
#   removes the dependency risk and makes every calculation auditable.
